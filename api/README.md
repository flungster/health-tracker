# health-tracker API

FastAPI backend for health-tracker. See the project root `README.md` for the
big picture; `AGENTS.md` at the project root documents architecture rules.

## Local development

```
cp ../.env.example ../.env        # adjust as needed
make install                      # from the project root
make api                          # uvicorn dev server on :8000
```
